import {config as dotenv} from 'dotenv';

dotenv();
export const config = {
    host: 'localhost',
    user: 'root',
    password: '123456',
    database: 'pdisdos'
};
/*export const config = {
    host: process.env.DB_HOST || 'bvpdyodrvg5vhdzsonia-mysql.services.clever-cloud.com',
    user: process.env.DB_USER || 'ul6cy3ibvcdds8u7',
    password: process.env.DB_PASSWORD || 'YpholCgpdieaVvfOAgCZ',
    database: process.env.DB_DATABASE || 'bvpdyodrvg5vhdzsonia'
};*/

/*export const config = {
    host: "localhost",
    user: "root",
    password: "123456",
    database: "pdisdos"
};*/